using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scale_small_script : MonoBehaviour
{
    // Start is called before the first frame update

    public float scaleMe;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //this.gameObject.transform.localScale = new Vector3(0.07f, 0.07f, 0.07f);

    }
}
